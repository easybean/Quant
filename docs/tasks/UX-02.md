# UX-02 资产与合约字段解释与选择

2026-09-17：完成、部署并浏览器验收。

范围：UniverseWorkspace与样式，保留后端合同与旧草稿字段。只看行情无需本页建档；本页用于研究池固定成员引用，不表示证券身份、行情映射或真实交易规则已核验。

- 12个美股字段增加用途、示例、填写依据与边界；市场产品保留现有类型兼容，并标明当前仅美股场景。
- 上市市场、纽约时区、草稿日历、数量单位提供选择；现有非标准值作为未核验选项保留。步长与币种提供可编辑候选，不能把候选误当普遍有效规则。
- 新建名称、交易代码、身份与生效日期为空，删除原示例证券与任意2024日期。安全随机生成编号是内部草稿标识，非官方ID、不会自动映射行情；编号更改仅由显式按钮触发。
- 身份、日历、日期、版本和交易步长在高级设置中；验证失败自动展开，保留字段错误与首错聚焦。规则生效日同时用于当前单段代码映射和规则；历史需要资料依据，不能自动认为上市日期。
- 旧衍生草稿线性/反向模型按存量字段恢复，新表单显式线性初值与展示一致；不开放其他市场能力。

验证：TypeScript/Vite build通过（压缩主包超过500kB有非阻断告警，未为消除告警扩大重构）。实际HTTP浏览器12条说明、空初值、选择与生成编号、高级错误展开/首错聚焦、合法草稿保存、390px无横向溢出及页面异常0通过。验收草稿 `2f3c11f5-cf0a-431e-a12a-4130fe3ca4f3`，名称“说明验收20260917”，不赋研究资格。

发布 `/home/davidou/quant/releases/ui-instrument-help-20260917/ui-dist`，备份 `/home/davidou/quant/backups/ui-instrument-help-20260917/ui-dist`；保留旧资源并原子切换index，回滚恢复备份index。无后端、原始数据、代理、同步或交易修改。

后端合同回归：`PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_instruments.py tests/test_universe_drafts.py`，11 passed。

截图：[表单说明](../acceptance/2026-09-17-fix/instrument-help.png)。
