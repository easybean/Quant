# P0-03 因子派生读取器的重复来源空结果修复

执行日期：2026-09-08

## 改动

- `load_derived_panel` 在没有可加载文件时，仍返回只含必要列的空 DataFrame，并保留：
  - `excluded_duplicate_symbols`：被多个 provider 文件隔离的证券代码；
  - `loaded_symbols`：空列表。
- 扩展重复来源回归用例，断言两个属性在空结果中都存在。

该路径只读取派生层 Parquet 文件；本次未读取、写入或合并原始行情，也未修改因子公式、UI 或 API。

## 验证

- `.venv/bin/python -m pytest tests/test_factor_lab.py::test_read_only_derived_loader_rejects_duplicate_symbol_provider_files -q`：**1 passed**。
- `.venv/bin/python -m pytest tests/test_api.py tests/test_factor_catalogue.py tests/test_factor_lab.py -q`：**18 passed, 1 skipped**。
  - 唯一跳过：`test_ta_representatives_are_calculated_per_symbol_without_volume_imputation`，本地隔离环境未安装可选 `ta` 包；无失败。

## 部署

无需部署：修改仅影响尚未开放的因子研究读取器，不在当前线上 API 或静态 UI 请求路径中。
