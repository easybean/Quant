# PROXY-01 — 服务器代理状态

状态：部分完成（Docker 系统代理待具备非交互式 sudo 后配置）。

- `davidou` 用户级 `mihomo.service` 已运行，且只监听 `127.0.0.1:7890`。
- 本地经该代理访问 Docker Registry 获得预期的 `401` 未认证响应。
- 订阅配置位于用户私有配置目录，权限已收紧为 `0600`；本仓库未保存订阅地址、节点或凭证。
- 现有本地服务检查：8501、8510 返回 HTTP 200；8511 返回 HTTP 404（端口可达但根路径没有路由）。
- Docker daemon 代理和镜像拉取未执行：当前 SSH 会话的 `sudo` 要求交互式密码。后续仅需在服务器终端完成授权，再以 `http://127.0.0.1:7890` 写入受限 Docker systemd drop-in 并重启 Docker。
