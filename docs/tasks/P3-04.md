# P3-04 新建回测与任务页面

执行日期：2026-09-09。状态：**界面与受控提交已完成；待服务器浏览器点击验收，不提前勾选。**

## 交付

- 新增 `GET /api/v1/backtests/availability`。它明确返回正式回测为不可用，以及原因：P3-03A 尚未提供包含稳定身份、原始价、可用时点、公司行为适用性及退市范围的合格资产池。响应不暴露数据路径、账户或凭证。
- “回测中心 → 新建回测”现为无终端向导，覆盖策略、资产池、日期区间、资金/成本、提交前检查五部分。正式模式只显示阻断项，不会发起任务。
- 仅可选择一个清晰标注为“合成验收样例”的路径：`ACME`、固定三根日线、买限价 `10 → 4@100 → cancel 6`、USD 1 首次成交费用。用户需显式确认它不是历史收益回测后才可提交。
- 向导复用 P3-01 的持久化任务 API 与 P3-03 的固定 `synthetic_daily_limit` 契约；前端提交期间禁用按钮并使用一次性 `Idempotency-Key`，意外重复点击不会创建两项任务。提交后轮询任务到完成，显示任务 ID、失败原因或已发布状态。
- “回测任务”说明同步为：只存在合成验收提交入口，正式回测仍锁定。

## 验证

```bash
PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_api.py tests/test_jobs.py tests/test_backtest.py tests/test_strategies.py
cd ui && PATH=/Users/douw/.nvm/versions/node/v20.19.3/bin:$PATH npm run build
```

结果：后端 **27 passed**；TypeScript 检查与 Vite production build 通过。

新增 API 测试锁定正式 `false`、P3-03A 阻断理由及唯一合成操作；既有任务测试覆盖合成提交、持久化完成、幂等键与正式回测 422 阻断。

## 边界与待验收

- 未新增真实行情读取、没有解除 P3-03A、没有编造收益或将合成账本标为历史回测。
- 已随 2026-09-09 后续前端发布切换至服务器；API/UI 健康检查均返回 200，未启动 Docker、未修改 raw/derived/curated 数据。
- 尚未保留一次服务器浏览器点击“合成验收任务”的流程证据；完成该点击验收前，`TODO.md` 保持未勾选。
