# P1-10 Yahoo 历史补数与每日增量更新

日期：2026-09-16。范围：Yahoo 日线获取、独立原始批次、当前合并视图、用户级 systemd 定时器。不购买数据、不绕过限流、不改变研究/回测资格门禁。

## 环境与当前阻碍

服务器 yfinance 1.7.0；已有 Nasdaq/Alpaca 数据，但无 Yahoo 日线目录、用户级定时器或下载进程。主表有 36,987 行，包含重复及退市生命周期；每日范围按 active Stock/ETF 筛选去重，不宣称历史全市场覆盖。

两次 AAPL 小样本探测均 0 行，包含 DNS 超时和 `YFRateLimitError`；IPv4 HTTPS 探测返回 429。停止手工反复请求，不采用代理绕过限制。尚未取得真实新增 Yahoo 数据。

## 更新合同

- 使用独立 `yahoo-daily-v1` namespace；当前部署逐股票从实际旧行情最后日期的下一天补到保守已收盘截止日，不限制两周或 100 个标的。没有旧行情的标的从 2016-01-01 获取；已有 Yahoo 视图按最近日期回查七天，并补齐此前短窗口遗漏的旧数据桥接日期。旧源与 Yahoo 不静默拼接，原历史与研究快照保持不变。
- 下载器返回的原始 DataFrame 先按不可变 run ID 保存并登记 SHA-256，再标准化并发布可变合并视图；同日期修订更新视图但原批次保留，旧研究快照不修改。缺失公司行为字段保持未知，不将其当作零事件。
- 截止日保守排除尚未结束的纽约日，不臆造交易日或历史 `available_time`。
- 进程锁防并发；失败保留旧文件，连续三次失败停止。摘要记录窗口、获取时间、成功/失败与延期，不记录凭证或异常全文。
- 全部 active Stock/ETF 去重进入同步队列；退市/非活跃证券保留历史，不要求更新到今天。单轮有 18 小时运行预算，预算结束明确 partial，可公平续跑；不以全队列启动声称全覆盖完成。

## 定时与运维

用户级 `quant-yahoo-daily.timer` 每日北京时间 11:30 后随机延迟最多五分钟执行。失败不热重启，`Persistent=true` 补跑错过的执行；开机持续运行依赖用户 manager/linger。

```bash
systemctl --user status quant-yahoo-daily.timer
systemctl --user list-timers quant-yahoo-daily.timer
journalctl --user -u quant-yahoo-daily.service -n 30 --no-pager
```

摘要：`data/manifests/yahoo-daily-v1/latest.json`。定时器正常不等于下载成功，受阻必须保留 failed/partial。新数据仍需质量审查和浏览索引刷新，不能自动成为合格回测输入。

行情浏览通过固定 JSON 索引读取，不在 HTTP 请求中扫描目录；新增离线五分钟刷新及服务退出后刷新。不同来源保持独立，同股票默认最新日期来源优先。失败可在 journal 与状态 JSON 中查询，尚未接入短信/邮件等外部通知。

回滚时使用 `systemctl --user disable --now quant-yahoo-daily.timer` 停止后续执行，并确认 oneshot service 是否仍在运行；必要时停止该 service。保留已取得的批次、状态与审计记录，不删除现有行情或固定研究快照。

## 验证与发布

- 本地及服务器隔离环境专项测试均 **9 passed**，编译与 `git diff --check` 通过；仅有 pandas 未来 concat 行为提示。覆盖修订留存、增量窗口、锁、熔断、限额、损坏旧文件、非法响应、CLI 退出码与异常记录恢复。
- 隔离发布目录：`/home/davidou/quant/releases/yahoo-daily-20260916-01/app`；预留回滚目录：`/home/davidou/quant/backups/yahoo-daily-20260916-01`。新增模块与用户级 service/timer 已安装，未覆盖现有下载器或重启 UI/API；生产模块 SHA-256 与本地一致。
- 定时器 `enabled` / `active`，`Linger=yes`；检查时下一次执行为 **2026-09-17 11:30:02 Asia/Shanghai**。服务配置通过 `systemd-analyze --user verify`，现有 API 健康检查通过。
- 为避免反复请求已限流接口，安装后未再手工启动下载服务；定时器首次执行尚未发生，生产 `latest.json` 将在运行后产生。今日探测真实新增 Yahoo 日线为 0，不能宣称历史数据已补齐。整项保持未完成，后续需正常响应后的真实补数、增量与浏览索引验收。

## 后续代理诊断与暂停

- 用户报告 VPN 订阅离线。mihomo 用户服务运行且本地端口监听，但经代理访问普通 HTTPS 网站 SSL 超时；普通网站直连 200，Yahoo 直连 429。
- 配置最后修改于 2026-05-20，仅一个静态 VLESS/Reality 节点，没有自动订阅源或到期信息。节点 TCP/TLS 可达不等于 VLESS/Reality 鉴权及转发正常；控制接口缓存 alive 不代表当前健康。未输出订阅链接、密钥或原始日志。
- 配置未启用 TUN，用户 systemd manager 没有代理环境变量，下载服务也未配置代理；此前漏查接入链路，下载直连限流与代理转发异常必须分开处理。
- 已临时停止 timer，下载 service inactive；timer 仍 enabled，用户 manager/机器重启可能再次启动。未改节点、订阅或下载环境。后续需有效代理配置和普通 HTTPS 转发验收，不通过更换出口绕过 Yahoo 限流。

## 代理恢复（后续用户授权）

- 用户提供有效订阅并明确要求重新配置代理、停止 Docker 排查。正常证书校验下订阅可返回 200；在内存中解码后，确认单个 VLESS/Reality 节点与当前配置的 `short-id` 不一致。UUID、公钥、SNI 与传输一致，`flow` 经空值归一化后也一致。不能把旧节点参数或瞬时请求超时归因于订阅永久失效。
- 只更新 mihomo 节点 Reality 参数，配置校验通过后重启用户级服务；备份 `/home/davidou/.config/mihomo/config.yaml.bak.quant-20260916T091733358543Z`，配置/备份权限 0600。订阅地址、UUID、公钥和 short-id 未写入项目文档、代码或 Git。
- 服务 active，端口就绪后，经本地 HTTP 代理访问 example.com 与 Cloudflare 均 200。Docker、整机路由和 TUN 未修改。宿主应用不会仅因代理监听自动继承代理；日线服务尚未接入代理，timer 仍临时停止，未通过代理再次请求 Yahoo 或恢复补数。

## 接入代理与两周真实同步（最新状态）

- 日线 service 明确设置本地 HTTP/HTTPS 代理，Requires/After 依赖 mihomo，并在执行前最多等待 20 秒检查代理端口。mihomo 已 enabled、active、Restart=on-failure，用户 Linger=yes；代理需常驻，退出登录后仍运行，机器关机时不能执行，恢复后 timer 可补跑。
- 经代理直接探测 AAPL/MSFT/SPY 均取得 2026-09-02 至 2026-09-15 的 9 行日线。随后通过正式 run_sync 落盘 MSFT/SPY 共 18 行并保存原始批次、SHA-256；AAPL 这轮空响应，摘要 failed（2 成功、1 失败），不宣称三个都落盘成功。
- 真实主表过滤出的 smoke 子集保存在服务器 metadata/yahoo-live-smoke-master-20260916.parquet，未改主表。正式 service 仍使用原 security_master，共 15,065 个 active Stock/ETF 去重标的；已启动后台补数，冷启动每轮最多 100 个，尚非全市场两周数据完整。
- 每次 attempt 原子写 running 进度，最终才写 success/partial/failed；连续三次失败熔断，失败不自动热重试、不切换代理节点。新增 bootstrap 参数、负数校验、运行中及熔断进度测试；本地及服务器隔离专项测试 **13 passed**（既有 pandas FutureWarning），systemd 配置验证通过。
- timer 恢复 enabled/active，每日北京时间 11:30 后最多随机延迟五分钟；核实时下一次为 2026-09-17 11:30:52。同步代码及 unit 已备份后发布，未重启 UI/API，也未修改 Docker。最新补数结果以 latest.json 和实际 parquet 为准；页面索引和研究资格仍未自动更新。

## 全表按各股票缺口补齐（取代上述两周/100 个临时范围）

- `sync_baselines.py` 每日离线盘点实际旧行情 date/source，不使用页面旧索引确定边界；首次盘点 10,330 个代码、0 个坏文件，排除 Yahoo 同步视图和合成数据。边界写 baselines.json，实际主表有 15,065 个 active Stock/ETF 去重标的，历史退市数据不列入必须日更范围。
- `daily_sync.py` 使用单标的 Ticker.history/raise_errors=True，保留共享旧下载器。仅当一次有界诊断得到 Yahoo 明确 Not Found 时，记 symbol_unavailable 而不全局熔断；未知空数据、网络、限流、校验/存储失败仍 fail-closed。没有伪造缺失行情，也不切换代理出口。
- 实际发现接口间日线时间戳 00:00 与 UTC 04:00 导致同日重复；已在同步器内统一日线日期后去重，原响应批次保留。再次真实验收：AAPL/MSFT 从 8 月 31 日至 9 月 15 日各 11 行；TSLA 从 9 月 1 日至 15 日 10 行，三只均零重复交易日，填补之前漏掉的桥接日期。
- 全表服务已改为无 symbol/bootstrap 上限、18 小时预算，读取每日生成的实际 baselines；预算耗尽保留 partial/deferred。baseline、同步、索引专项测试最终累计 **30 项**（同步 23、索引 5、盘点 1、覆盖 1），受影响测试已分别在本地及服务器隔离环境通过；既有 pandas/FastAPI 警告保留。
- `quant-yahoo-catalogue.timer` enabled/active，离线刷新后生成 coverage.json，逐活跃代码报告旧末日、新末日、是否桥接及 endpoint_current/needs_update，另计 historical_only_symbols；端点当前不是交易日连续性或研究资格合格证明。
- 行情页面删除开始/结束日期输入，不再写死 2026-08-31；按选定来源最新日期展示，周期支持 1个月/3个月/1年/全部。全部历史分段读取，保留服务端单次 366 天/300 行保护而不截断总历史。构建通过，静态文件备份后发布；网页 API 已验证 TSLA 新来源优先且可读至 9 月 15 日。浏览器验收工具连续两次超时，未宣称视觉流程验收通过。
- 保留每天 11:30 的全表增量定时器及常驻代理；另设置当前任务半小时跟进全量覆盖与故障，运行正常无重大变化保持安静。全表尚未全部补齐，不勾选任务完成，最终以 coverage 与失败记录核验。

## 修复同一股票拆成两个搜索结果

- 浏览层按同一 symbol_key 的股票身份生成 `us-daily:<symbol_key>` 展示视图，股票搜索只返回一个结果；不同身份不能只因 ticker 相同而混合。原单源 ID 保留兼容，原始文件、每日采集与研究门禁不变。
- 基准历史优先保留，Yahoo 仅追加基准真实最后日期之后的日线；同日不覆盖旧价格、不平均混源。响应逐行保留来源和口径，并提供 source_segments；视图标为 `display_composite_unverified`，只用于行情浏览，不能视为合格回测/总回报/PIT 数据。每次仍限 366 天、300 行、8 个来源文件；全部历史由页面分段读取。
- 页面搜索结果按股票计数，移除 provider/namespace 当作不同股票的选项；来源显示在明细与图表十字线中，Nasdaq 历史不再展示为 unknown，跨分段读取累计保留来源元数据。
- 本地和服务器隔离专项测试 **7 passed**（既有 FastAPI 警告）；UI 构建通过。真实 TSLA 全部历史验收 **2,523 行**、2016-08-31 至 2026-09-15、无重复日期；8 月 31 日 Nasdaq 与 9 月 1 日 Yahoo 正确接续。
- 隔离发布 `/home/davidou/quant/releases/unified-quotes-20260916-01`；模块及静态文件回滚备份 `/home/davidou/quant/backups/unified-quotes-20260916-01`。只重载行情 API，下载服务/代理与原始数据不改变。浏览器视觉验收仍未完成，构建与实际 API/全历史读取已验证；全市场采集完成度仍以 coverage 清单为准。
