# P0-01 真实因子页面验收与必要修复

2026-09-17补验：独立Chromium真实分类/搜索/详情联动、空结果清空、503失败无演示回退及重试恢复通过；当前目录254项/9类、Alpha360隐藏，API状态契约一致。原浏览器阻断已解除，本任务完成。详情见 [统一验收](ACCEPT-20260917.md)，下方保留历史实施记录。

执行日期：2026-09-08

## 范围与结果

- 修复因子百科搜索后的详情联动：选中项现在必须在当前分类和搜索结果中；搜索无结果时清空详情，显示空状态。
- 切换分类时先清空选中项，再由当前可见列表选定首项，避免短暂显示旧分类详情。
- API 状态严格由 `runnable` 推导：可计算表示 `runnable=true`；不可运行或待校准均为 `runnable=false`，其中待校准保留原有状态。
- 保留加载失败提示与重试入口；未添加演示数据回退，也未开放研究计算。

## 改动文件

- `ui/src/components/FactorCatalogue.tsx`
- `src/quant_data/api.py`
- `tests/test_api.py`

## 验证证据

- 本地目录构造检查：250 项、9 个非空分类、无 `qlib360_` 条目、15 个当前不可运行/待校准条目。
- 本地 API payload 检查：250 项、9 类、Alpha360 隐藏，所有条目满足 `runnable == (status == "可计算")`。
- 前端：使用 Node 20.19.3 运行 Vite 8.2.2 生产构建，通过；产物为 `ui/dist`。
- 服务器发布后本机检查：`/api/v1/health` 返回 `{"status":"ok","schema_version":"v1"}`；`/api/v1/factors` 验证 250 项、9 类、Alpha360 隐藏及状态一致性；API/UI 两项用户服务均为 `active`；静态首页引用 `assets/index-ud0E9XI2.js`。

## 发布与回滚

- 版本：2026-09-08 P0-01。
- 备份：`/home/davidou/quant/backups/p0-01-20260908T0958CST`；切换前静态站点仍保留在 `/home/davidou/quant/app/ui-dist.pre-p0-01-20260908T0958CST`。
- 发布：替换 `src/quant_data/api.py`，重启 `quant-workbench-api.service`；原子切换 `ui-dist`。
- 回滚：从上述备份恢复 `api.py` 与 `ui-dist`，然后重启 `quant-workbench-api.service`。

## 遗留项

- `python3 -m pytest tests/test_api.py -q` 未运行：本机 Python 环境未安装 `pytest`。这由 P0-02 统一处理。
- `pnpm build` 因受限网络无法解析 pnpm 版本，改用已安装依赖及 Node 20 的 Vite CLI 构建成功。
- 浏览器交互验收尚待补做：自动化在打开已部署页面时两次于工具层 30 秒超时，未获得页面可访问性树。部署后 API 与静态构建已验证，但搜索/空结果的真实浏览器点击流程不能据此宣称已验收。
