# P0-02 测试环境与部署基线

执行日期：2026-09-08

## 环境基线

| 环境 | Python / Node | 关键测试与服务依赖 |
| --- | --- | --- |
| 本地隔离 `.venv` | Python 3.11.6 / Node 20.19.3 | pytest 8.4.2、FastAPI 0.115.13、Starlette 0.46.2、httpx 0.28.1、uvicorn 0.34.3、pandas 2.2.3、pyarrow 20.0.0、Streamlit 1.46.1 |
| 服务器 `/home/davidou/quant/venv` | Python 3.10.12 / Node 22.22.0 | pytest 8.4.2、FastAPI 0.115.13、Starlette 0.46.2、uvicorn 0.34.3、ta 0.11.0、pandas 2.3.3、pyarrow 20.0.0、Streamlit 1.62.0、httpx 0.28.1 |

`fastapi.testclient.TestClient` 需要 `httpx`。`pyproject.toml` 的 `dev` extra 现已显式加入 `httpx>=0.28,<1`，使以下本地测试环境可复现：

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev,api]'
.venv/bin/python -m pytest tests/test_api.py tests/test_factor_catalogue.py tests/test_factor_lab.py -q
```

本次为避免影响现有 Streamlit/API 服务，本地使用隔离 `.venv`；服务器仅新增 TestClient 所需的 `httpx`/`httpcore`，未升级 FastAPI、Starlette、Uvicorn 或 Streamlit。

## 实际验证

- `.venv/bin/python -m pytest tests/test_api.py tests/test_factor_catalogue.py -q`：**11 passed**。
- `.venv/bin/python -m pytest tests/test_api.py tests/test_factor_catalogue.py tests/test_factor_lab.py -q`：**17 passed, 1 skipped, 1 failed**。
  - 跳过：`test_ta_representatives_are_calculated_per_symbol_without_volume_imputation`，隔离环境未安装 `ta`；`ta` 已是项目运行依赖，完整 `pip install -e '.[dev,api]'` 会安装项目基础依赖。
  - 失败：`test_read_only_derived_loader_rejects_duplicate_symbol_provider_files` 期望 `excluded_duplicate_symbols == ["ABC"]`，实际空 DataFrame 缺少该属性。未在本任务修复因子/数据加载逻辑，留给后续独立任务处理。
- 服务器本机 HTTP 检查：旧 Streamlit `8501`、新静态 UI `8510`、只读 API `8511/api/v1/health` 均返回 **200**；`quant-workbench.service`、`quant-workbench-ui.service`、`quant-workbench-api.service` 均为 **active**。

## 可复用发布与回滚

1. 在本地隔离环境完成测试；新 UI 使用项目已安装的 Node 20 执行 `node node_modules/vite/bin/vite.js build`。
2. 服务器切换前将 API 源码和 `ui-dist` 复制到带时间戳的 `/home/davidou/quant/backups/<release>`。
3. 先校验待发布 API：`/home/davidou/quant/venv/bin/python -m py_compile <candidate>`；然后切换 API 并重启 `quant-workbench-api.service`。静态 UI 上传到临时目录后原子改名为 `ui-dist`。
4. 在服务器本机检查 `8501/`、`8510/`、`8511/api/v1/health` 均为 200，且三个用户服务均为 `active`。
5. 回滚时从该 release 备份还原 API 与 `ui-dist`，并重启 API 服务；P0-01 发布备份位于 `/home/davidou/quant/backups/p0-01-20260908T0958CST`。

## 状态

P0-02 已完成：当时发现的因子实验失败已由后续 [P0-03](P0-03.md) 修复；当前本地专项为 18 passed、1 个可选 `ta` 跳过。服务器已具备 TestClient 依赖并实际通过 API 测试。

## 2026-09-09 服务器测试基线尝试（受阻）

- 测试前基线：`/home/davidou/quant/venv/bin/python` 为 Python `3.10.12`；`pytest=8.4.2`、`fastapi=0.115.13`、`starlette=0.46.2`、`uvicorn=0.34.3`、`streamlit=1.62.0`，`httpx` 未安装。
- 仅尝试安装锁定测试依赖：`/home/davidou/quant/venv/bin/python -m pip install 'httpx==0.28.1'`。服务器当前可见包源返回 `Could not find a version that satisfies the requirement httpx==0.28.1 (from versions: none)`，没有安装任何包；按约束未改用其他版本、未升级服务依赖、未重试。
- 因 `TestClient` 依赖未满足，未同步或执行 `tests/test_api.py`，不能将本地通过结果代替服务器实际测试。
- 安装失败后复核版本与前述基线完全一致，`httpx` 仍为未安装；`quant-workbench.service`、`quant-workbench-ui.service`、`quant-workbench-api.service` 均为 `active`，服务器本机 `8501/`、`8510/`、`8511/api/v1/health` 均返回 `200`。
- 回滚/卸载：本次没有写入或升级任何分发包，无需卸载；后续若包源恢复并成功装入 `httpx==0.28.1`，可用 `/home/davidou/quant/venv/bin/python -m pip uninstall httpx httpcore` 回到本次基线（仅在确认这两个包未被其他新增测试依赖使用时执行）。
- 阻断解除条件：在不替换或升级 FastAPI、Starlette、Uvicorn、Streamlit 的前提下，向服务器配置可提供 Python 3.10 兼容的 `httpx==0.28.1` 的受控包源或离线 wheel；随后重新记录版本、同步无密钥的 API 测试文件并实际执行，再复核服务健康。

## 2026-09-09 国内镜像收口

- 清华 PyPI 镜像可用；服务器在不升级服务核心包的前提下新增 `httpx==0.28.1`、`httpcore==1.0.9`。
- 无凭证的 `tests/test_api.py` 已同步到 `/home/davidou/quant/p2-05-nautilus/test_api.py`，在应用目录以服务 venv 执行：**5 passed**。
- 复核 `quant-workbench.service`、`quant-workbench-ui.service`、`quant-workbench-api.service` 均为 `active`；`8501/`、`8510/`、`8511/api/v1/health` 均返回 `200`。
- 如需回到此前服务器测试基线，可在确认没有其他测试依赖后卸载 `httpx` 与 `httpcore`；生产服务本身不依赖这两个新增包。
