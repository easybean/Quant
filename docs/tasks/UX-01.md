# UX-01 已部署表单统一必填与字段级校验

状态：2026-09-17已修复、已部署并复验通过。此前发现记录见 [统一验收](ACCEPT-20260917.md)。

范围：资产与合约、数据集与资产池、策略模板草稿、全局风控、数据源声明、模拟账户；现有保存功能通过，但必填标记、字段提示与聚焦不完整。修正总览顶部等过时状态声明，不开发新业务、不改变服务端研究/风险合同。

验收：标识必填/必选；提交缺项与非法范围显示具体字段错误并聚焦首错；前置条件说明与直达入口；提交中防重复；后端独立422拒绝仍保留；接口失败不回退演示；创建/更新/历史回归与浏览器证据齐全。

P3-04的HTTP兼容故障独立记录在P3-04，本次一并发布。

## 修复与证据

- 六类 Workspace 使用共享 FormValidation 必填、ARIA、字段提示与首错聚焦；服务端校验独立保留。合法日期拒绝自动滚入下一月的日期；模拟账户金额保留精确十进制文本，不经浮点转换。
- 资产池缺成员定义时保存仍可校验，并提供“资产与合约”直达入口。总览与顶部过时未接入文案已纠正。
- `npm run test:unit` 两个测试文件共10个用例通过；使用现有Codex Node运行时 `npm run build` 通过。系统Node19不支持当前Vite所需styleText，未升级系统环境。
- 已部署HTTP页面六类表单缺项、非法范围、首错聚焦、创建通过；五类版本化草稿更新v2且历史各2版，模拟账户创建与空台账通过。后端422字段映射通过。缺前置条件单独复验通过：首次脚本误用按钮名称导致超时，修正定位后通过，不是产品故障。
- 证据：[主流程](../acceptance/2026-09-17-fix/main-results.json)、[前置条件复验](../acceptance/2026-09-17-fix/prerequisite-results.json)。测试草稿名称带“修复验收20260917”，未赋予研究资格、未下单。
- 部署：`/home/davidou/quant/releases/ui-http-forms-fix-20260917-01/ui-dist`；备份：`/home/davidou/quant/backups/ui-http-forms-fix-20260917-01/ui-dist`。保留旧哈希资源、原子切换index；回滚恢复备份index即可。不重启API或同步任务。验收后仅补充资产池切换/成功校验时清空过期错误并重新构建发布。
