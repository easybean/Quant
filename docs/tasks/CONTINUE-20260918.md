# 2026-09-18 持续交付：免费美股采集与合成报告

范围：继续已授权的免费美股数据修复、日更和回测报告链路。不涉及其他市场、实盘、付费数据或大规模重构。

## 当前事实与修正

- 既有15,058个计划内代码均有末日采集尝试；2,395个末日缺口不是未跑任务。官方当前上市目录交叉核对其中470个仍在名单，1,925个未找到。未找到只标记生命周期未知，不判定退市、不移除缺口。
- 当前官方目录额外发现103个旧主表完全没有的代码。发布追加型采集清单：保留原始24,516行及历史生命周期，追加103行；活跃15,168个，隔离7个ATEST后计划范围15,161个。后续目录刷新保留此前发现的代码，不因今天目录缺席而删除。此清单不是合格历史研究池。
- `listing_reconciliation.py` 归档两个官方原文、来源footer与SHA；两份来源日期须一致、非未来且不超过七天。来源文件日期不等于已知UTC发布时间，观察时间也不等于历史可用时间。
- `asset_reference.py` 仅GET固定 `/v2/assets` 证券元数据；禁止重定向转发凭证，不访问账户/订单。实际归档33,509条当前/非活跃供应商记录，用于当前符号交叉验证，不授予交易权限或历史身份资格。
- `sync_reference_refresh.py` 顺序采集目录、资产元数据、建立映射；成功后才发布固定映射hash指针。每日服务器任务不调用模型。
- 新采集修订记录真实UTC `retrieved_at/available_at`，策略不能把今天获得的历史修订默认当成当年已知。

## 合成报告边界

`backtest_reports.py` 与网页只读已有已发布合成工件。核对固定任务路径、工件索引归属、大小、符号链接、内嵌报告hash、Decimal现金/持仓/成交/费用/PnL勾稽。完整文件SHA仅为读取时内容指纹，不是与事先批准hash核验的证据。

对比必须匹配完整bar内容hash、日期、初始资金、币种、价格口径、数据/资产池/日历、引擎及订单/成本合同。只比较期末账本；不存在的净值曲线、回撤、Sharpe和基准明确不提供，不伪造策略研究收益。

## 仍不能验收为完成的研究能力

- 免费当前证券名单/供应商UUID不能证明完整历史稳定身份、历史资产池成员或所有代码复用。
- 现有公司行为代表样本不证明全市场无遗漏；退市最终兑付/换股缺失不能用最后价格或零收益替代。
- 现在下载历史数据只有现在的观察可用时间；缺少当年发布/修订记录时，不默认历史PIT成立。
- 完整历史区间缺口与日更连续性仍须实际数据证据；请求成功、服务active、末日到达都不等于历史连续完整。
- 因此P1-10/P1-11/P2-06B完整研究资格/P3-03A及真实正式回测保持未完成。报告页面的合成阶段交付不等于P3-05真实策略风险/基准报告全部完成。

## 部署与验证

- 已部署 `/home/davidou/quant/releases/us-research-progress-20260918-02`，完整Python包与静态目录备份 `/home/davidou/quant/backups/us-research-progress-20260918-02`。完整目录切换，失败恢复整包/前端assets/服务配置，EXIT恢复同步timer；原始行情和主表从未替换。
- 日参考刷新07:20 Asia/Shanghai；证券目录07:25；全量行情原续跑timer保留。服务超时从25改45分钟，覆盖新增映射12分钟、标准SIP12分钟、Yahoo4分钟、Nasdaq1分钟预算及固定开销；限速和重试门禁不放宽。
- 映射初次实际251条；再加入独立完整名称+市场唯一UUID候选检索（不模糊匹配）后发布310条，拒绝157条。数字/类别/系列/票息保留；`Limited Duration` 不当作公司后缀删除。同名多UUID/市场拒绝。只用于当前31日尾窗，较早历史单独保留待核验任务。
- 首次实采发现批量下载器要求original symbol，上层传provider symbol；首3项失败后停止映射阶段。修正接口并增加默认batch集成回归后继续采集；仅解除该已确认接口故障冷却，未清空历史记录或尝试预算。新记录真实UTC `available_at` 对Yahoo与恢复源均生效。
- 后端报告21 passed；映射初版及相关链路53 passed，扩展候选/恢复/调度/刷新48 passed；接口修复恢复与batch30 passed；Yahoo33 passed；覆盖/调度10 passed；参考/摘要10 passed、证券目录7 passed。部分测试集合有重叠，不将这些数字相加伪称唯一总覆盖。
- UI19项单测通过、最终production build通过（已有>500k chunk警告）。部署3份已有合成报告API/详情/对比/非法ID/503重试/390px无溢出通过；全部38导航标题/用途/面包屑再次通过，无pageerror。见 [报告验收](../acceptance/2026-09-18-research-progress/results.json)。
- 00:53 CST阶段统计：计划15,161，末日当前12,965，仍待更新2,196；映射新成功247条全部实际末日2026-09-16。此为阶段数，不表示全表或完整历史验收完成。

## 最终修复与末日阶段验收

- 两批映射实采248/248、75/75成功，合计323个唯一代码全部实际末日2026-09-16。不同版本的严格匹配范围有变化，323是采集结果不是310条当前映射的同一统计口径；旧映射与原始档案保留。ABR$D/E/F的页面消费API均返回同一原始代码、末日9月16日。
- 00:55:53 CST覆盖：15,161个计划代码，13,073末日达到目标、2,088仍待更新。固定官方目录核对剩余163在名单、1,925不在名单（仍未知，不当作退市）。
- 后续真实链路又发现：历史映射待核验义务有较早requested_end，Yahoo当前价队列严格要求全部end=target，混入会产生配置ValueError。修复：所有当前价格provider队列隔离此类审核义务、仍保留原始历史待核验清单与任务；不同映射版本同symbol只生成一个审核任务，不删除其他原始义务。不是把旧历史默认补齐，也不隐式扩大窗口。
- 摘要的单一provider阶段配置异常不再导致全部已知计数消失；异常来源次数显示未知/null，不伪造0。16项调度/摘要专项通过。最终版本 `/home/davidou/quant/releases/us-research-progress-20260918-03`，完整回滚 `/home/davidou/quant/backups/us-research-progress-20260918-03`；其他五个unit使用02发布的已验证配置。
- 最终部署后报告/对比/503重试/手机验收再次通过；摘要run_available=true。Yahoo当前实际队列截止日全部9月16日，新批次已实际尝试12项（1成功、11来源unavailable），不再因历史审核截止日冲突失败；240秒预算后继续由timer恢复全表任务，不宣称免费来源无缺口。
- 采集清单额外保存前版继承输入relative_path/hash，使后续日追加可回溯原始master及不可变派生版本。原始主表、退市行与旧配置快照不改。

回滚时先暂停相关服务，在停机窗口恢复备份的完整 `quant_data` 和 `ui-dist` 目录及五个unit，daemon-reload、启动API做health、恢复timer。新增参考快照/原始行情不删除；证券目录数据库如需回滚使用SQLite backup，旧股票池快照仍保留。不能只回滚index却保留不匹配的API版本。
