# Massive 免费批量日线接入

2026-09-18：用户提供服务器凭证并批准真实验证与每日同步。未改变套餐、未购买数据、未触交易账户/订单接口。权限请求只使用数据 GET。

## 真实接口证据

9 月 17 日 grouped 日线真实返回成功：12572 行、基础 OHLCV 检查接受12572、拒绝0。原始响应1373747字节，SHA-256 `4a266c7263faa199423d6a87acff9ac93afd1e61393dabb266fd665e4a374b34`。

固定原始快照：`reference/massive-daily-v1/20260918T063308Z-93cf56c9fbf44b799fb96be8053059b4`，观察时间 `2026-09-18T06:33:08.736987+00:00`。明确 adjusted=false、include_otc=false；available_at 取实际观察时刻，不倒填到9月17日。报价日期/成交筛选定义、历史身份、公司行为、许可与退市适用性仍不代表研究合格。

与采集清单15161代码比较：12139精确活跃匹配，433供应商代码未匹配，3022清单代码未返回。不能以标点替换/模糊匹配自动合并代码，也不删除清单缺口；未匹配证券仍保留在原始响应中。TSLA/MSFT/SUPX/AAPL 均实际返回9月17日日线。

服务器 `/home/davidou/massivekey` 原权限664，已收紧至600；密钥不进仓库、日志、网页或 manifest。

## 交付边界

`massive_daily.py` 保留不可变原始响应/诊断/有效行；`massive_publish.py` 只将现有采集清单精确匹配成员发布到独立 provider=massive/namespace=massive-daily-v1，每个版本保持输入血缘。无研究/回测资格变化。

浏览器追加白名单与覆盖/调度读取该 namespace，但保留旧来源优先级和完整历史；只追加图表末尾可用数据，不原地覆盖旧供应商历史。单只证券搜索仍仅返回一个行情视图；跨源价格口径未核验警告保留。

每日任务计划10:00 Asia/Shanghai，两次有界机会11:00、12:00；同一完成交易日重复运行不再次采集。休市沿固定日历取完成交易日，proxy由mihomo常驻依赖提供；模型不参与定时任务。既有15161代码完整历史补数及其他免费源续跑继续。

这不是“历史全量补齐”或“全部证券可交易”的验收；任何公司行为/身份/PIT/退市缺口仍保留研究门禁。

## 验收与部署

消费链首次专项26 passed，新增独立Massive状态与桥接缺口用例后受影响状态6 passed、覆盖2 passed；集合有重叠，不累加为独立覆盖。UI build成功，既有500KB chunk与FastAPI startup弃用警告保留。

发布器专项3 passed、适配器8 passed。真实发布于2026-09-18T06:41:13Z完成：attempted/success/catalogue_updated均12139，failed=0；保留433未匹配、3022未返回。覆盖盘点截止9月17日：末日当前12459、仍需更新2702，不代表桥接窗口或历史完整。

只读浏览器验收通过：ZTS精确证券搜索一个结果，末日9月17日，实际bar一行，日/周/月切换无pageerror；模糊搜索可返回其他不同证券，不作为重复。验收脚本及结果在 `docs/acceptance/2026-09-18-massive/`。

部署目录 `/home/davidou/quant/releases/massive-daily-live-20260918-01`；完整旧包/UI备份与预切换目录在 `/home/davidou/quant/backups/massive-daily-live-20260918-01`。脚本失败会停新任务并恢复完整旧包/UI，已独立发布数据保留。API、旧全量续跑timer与mihomo均active；新timer active，下次北京时间9月19日10:00；密钥权限600。

实际systemd service重跑于06:48:07Z完成，Result=success、ExecMainStatus=0；captured=false、attempted=0、skipped=12139、failed=0，复用全部已验证输出并刷新12139索引，未重复采集。文件合同/hash与索引修复全量校验约6分钟，CPU计7分54秒；未优化掉安全检查，后续若需优化应保留完整性语义。06:48:19Z覆盖刷新成功。首次日线接入和重跑验收完成，但未来连续多日无人值守与历史全量完整性仍待验收。
