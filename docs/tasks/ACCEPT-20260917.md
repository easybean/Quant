# 2026-09-17 已实现、已部署功能统一验收

时间：2026-09-17，浏览器证据始于17:23北京时间。验收基线：提交 `f462587`，服务器 `/home/davidou/quant/app`，UI8510/API8511。

## 范围与方法

用户授权验收已有功能并更新TODO，不开发新业务、不切换服务、不解除研究/交易门禁。Git工作树开工时干净。本轮修改仅为任务状态、记录与证据图片；原始行情、已有用户草稿、密钥及后台补数服务保持不变。

桌面内置浏览器/Chrome控制各一次工具层30秒超时后停止尝试。使用独立headless Chromium（已有chromium-1169）与Playwright真实访问线上页面，1440×1000桌面视口。测试工具在临时目录，未加入项目依赖；临时安装了playwright-core，不升级项目环境。未做手机/全浏览器兼容、连续多日日更或外部账户验收。

浏览器脚本位于实施时的 `/private/tmp/quant-acceptance.C1CtKN/`（临时路径不保证长期存在）。永久保留本记录、实际数据库/任务ID和下述截图。先按页面真实标签定位；首次脚本等待时序、风控选择器与回测结果接口假设有误，已修正，只对受影响项复查。脚本失败不能直接算产品失败；下表以复查后的证据为准。

API、草稿/资产池/风控/数据源/台账/可视化策略、backtest/factor_jobs/factors/jobs等11个Python模块SHA-256与本地一致。既有同版本专项/黄金测试证据沿用，没有为盘点而重复运行全套测试。后端实时验收七个配置创建接口空JSON均返回422；不写入无效配置。

## 功能验收结果

| 功能/任务 | 实际验证 | 结论 |
|---|---|---|
| B-02/B-03、P0-01 因子目录 | 当前254项/9类，Alpha360隐藏，runnable与可计算状态一致；分类/搜索/选中详情联动、无结果清空详情；拦截503显示失败且无演示回退，解除拦截后点击重试恢复 | 原缺失浏览器验收补齐，P0-01完成 |
| P1-05 数据目录 | 固定盘点/质量版本、来源与边界显示；点击质量分类仅显示对应一项 | 页面功能通过；数据盘点仍为9月8日旧快照，不能当作实时补数报告 |
| P1-06 总览 | 真实254因子、在线API、固定数据快照；组合净值/实验空状态，没有演示收益 | 通过当前只读快照范围；顶部“数据接口未接入”与正文在线API并存，文案欠账需修正 |
| P1-07 数据源声明 | 网页创建、更新到v2、历史两版，只有无效验收引用名、不填凭证 | 功能通过，仅声明不代表供应商接入已验证 |
| P1-08/09 行情浏览 | TSLA唯一搜索结果，读到9月16日，252根1年日线；实际7个图表canvas、十字线/来源显示、鼠标滚轮操作，无开始/结束日期输入 | 通过本次浏览范围；未重新逐段验收全部历史/所有公司行为口径 |
| P2-06/06A 标的/资产池 | SPY代码测试标的保存v1/v2；引用固定标的v2的资产池保存v1/v2；API读取历史各两版 | 已部署且功能通过；验收身份不是官方证券身份，不授予研究资格 |
| P3-02/02A 模板草稿 | 买入持有/SPY参数网页保存v1/v2并读取历史 | 当前模板配置保存通过，不代表真实策略运行 |
| P3-02B 可视化策略 | 标的→资产池v2→因子规则→策略v1/v2→历史；8处必填标记，缺名/缺池提示、首错聚焦；不可执行边界清晰 | 补齐保存闭环，完成定义层验收 |
| P3-01 后台任务 | 两个合成模板API提交成功；同幂等键重复提交返回同ID、created=false；页面任务/实验列表显示已发布 | 通过本轮发布/幂等路径；取消/重启恢复沿用既有证据，本轮不重启服务 |
| P3-03 合成回测 | 两个模板产物实际落盘；成交4、撤6、费用1、现金599、持仓4；合成价格102，未实现PnL8、总PnL7 | 合成声明成交黄金路径通过，不是完整由策略信号驱动的真实成交模拟；正式回测继续阻断 |
| P3-04 回测网页 | 正式回测门禁清晰；点击合成模式、确认、提交后报crypto错误且未发提交请求 | **失败，保持未完成** |
| P4-01/02 因子可用性/研究页 | 校准模块已部署，状态契约一致；无合格快照available=false/snapshots=[]；网页显示研究计算已阻断，不显示提交入口 | 门禁与配置层通过；真实计算/收益未验收 |
| P6-00 风控草稿 | 网页创建、更新到v2、读取两版历史；不会执行停单/撤单 | 功能通过，仅非执行草稿 |
| P6-01 模拟账户/台账 | 网页创建USD1000空账户、读取空台账，初始/账本现金1000；无示例订单/成交/PnL | 配置/空台账通过；事件不可变/去重沿用既有测试，本轮不追加事件 |

P0-02/03、P1-01至04、P2-01至05等没有新的待验收线上交互，保留已有只读盘点/模型/黄金证据，不因本轮验收扩大能力。P3-03A固定数据资格、P1-10/11全量补数、P3-05报告及其后的未实现任务保持未完成。

## 已确认的问题

1. **P3-04 HTTP提交故障**：`http://192.168.1.132:8510/` 中 `isSecureContext=false`、`typeof crypto.randomUUID='undefined'`。确认合成验收后提交，页面“无法继续 / crypto.randomUUID is not a function”。位置：`ui/src/components/BacktestWizard.tsx`；`FactorResearch.tsx` 同样调用此API，但真实研究入口当前被门禁阻断，本轮不开放它来试。需兼容HTTP环境生成幂等键，并复验实际页面提交、失败和防重复点击。后台API成功不替代前端验收。
2. **UX-01 六类旧表单不符合统一提示要求**：资产定义、资产池、模板、风控、数据源声明、模拟账户的统一`.required-mark`和HTML `[required]`均为0。模板部分参数有“必填”文字，但草稿名等仍不完整；其他页面主要依靠顶部服务端错误，无统一字段级校验与首错聚焦。后端空输入422已验证；保存成功不等于UI规范完整。资产池无标的时保存按钮仍禁用，虽有前置说明但缺直达入口，需一并处理。
3. **状态文案**：总览顶部“数据接口未接入”是静态文案，与正文API在线不一致；“尚未建立实验记录库”等历史空状态文案也未随已有后台基础同步。后续只修声明，不伪造运行状态或收益。本轮实际创建合成实验后，任务/实验专页已有真实记录；总览仍是固定快照/空状态设计，不能声称实时汇总。

本轮仅验收与记录，未修上述代码，P3-04及新增UX-01明确待修。没有新部署，因而无服务切换/代码回滚操作。

## 可复核工件与副作用

只创建带 `[验收20260917]` 前缀的非执行记录，保留供复查，未覆盖已有用户记录、不删除审计数据：

- 标的：`d1c62fbe-54ed-4e56-a9cf-729f155ad14e`，身份键 `ACCEPTANCE-SPY-20260917`，仅验收，不作官方身份声明。
- 固定资产池：`5f20fb55-1993-4444-89b2-4c3a7220e608`，v2。
- 可视化策略：`a84fc572-67a5-4b22-829a-5b89258a9125`，v2，固定引用资产池v2。
- 模板草稿：`bf4d449d-2a56-4794-853e-bd503c0365bb`，v2。
- 数据源声明：`b241d828-1655-4331-94ae-20336ed09d7d`，v2，引用名 `ACCEPTANCE_NO_CREDENTIAL`（无实际凭证）。
- 风控草稿：`11195cff-1589-4d5d-9409-f6a9e1f31c4f`，v2。
- 空模拟账户：`a76d8768-6d8d-4e79-8263-a4cd51b34b42`。
- 合成任务买入持有：`30207546-dd0d-467f-9917-9c422653c24c`；双均线：`e8afb6eb-c869-4573-8839-65395b25c52f`，code_version均为`acceptance-20260917`。实际报告位于服务器 `data/workbench-state/artifacts/<ID>/backtest-report.json`，仅合成输入和声明成交。

截图：[必填与首错提示](../acceptance/2026-09-17/strategy_required.png)、[策略版本历史](../acceptance/2026-09-17/visual_strategy_versions.png)、[TSLA统一行情](../acceptance/2026-09-17/market_chart.png)、[回测故障](../acceptance/2026-09-17/backtest-failure.png)、[实际任务列表](../acceptance/2026-09-17/tasks.png)、[实际实验记录](../acceptance/2026-09-17/experiments.png)。

## TODO变更

P0-01、P3-02B改为完成；纠正P2-06A/P3-04/P4-01/P6-00/P6-01过时的未部署声明；同步当前254因子及去除演示总览的事实。P3-04保留未完成并增加故障证据；新增UX-01记录表单/声明一致性近期欠账。全量补数和真实研究门禁状态不变。
