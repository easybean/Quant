# Quant 协作、研究与交付约定

本文件适用于本项目后续所有任务。用户明确要求：以必要的模型调用完成可靠交付，不必反复提醒“省用量”；节省不能成为跳过安全检查、掩盖失败、降低事实准确性或量化正确性的理由。

质量优先级固定为：**Research Correctness > Data Correctness > Backtest Correctness > Engineering Elegance > Development Speed**。工作量大不等于推理难度高；模型升级依据是推理复杂度、研究风险、系统风险、不确定性和已有失败证据，而不是代码行数。

## 1. 规则优先级与事实来源

1. 系统/用户当轮指令、权限边界和安全要求优先于本文件；用户指定模型、工作方式或范围时从其要求。
2. 本文件规定协作、研究正确性和工程质量；`TODO.md` 是任务状态入口，具体任务记录在 `docs/tasks/<ID>.md`。
3. 产品目标以 `docs/product-system-architecture.md` 为准，但它是设计提案，不可当作已实现或已授权的功能。当前真实状态优先级为：运行状态/代码/测试 > 任务记录 > `TODO.md` 与 `README.md`。`docs/CODEX_HANDOFF.md` 记录截至 2026-09-16 的盘点及已知差异，应按需核实。
4. 不因本文件而授权实盘、外部账户、付费数据、购买服务、部署或破坏性操作。所需权限、数据或账户缺失时，完成可验证部分并清晰标记阻断，不提前宣称完成。

## 2. 模型路由与 Agent 分工

### 当前可选性与目标路由

- **当前平台实际可选模型**以调用工具当时列出的模型为准；本环境已列出 `gpt-6-astra`、`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna` 与 `gpt-5.5`。不要假定未列出的模型可以调度。
- **目标路由**：高能力模型定义问题、研究、推理、架构、审查和高风险决策；高速/低成本模型实现明确方案、测试、ETL、数据处理和机械工作。主模型负责范围、架构/风险判断、任务拆分、关键审查与最终结论。
- **GPT-5.3-Codex-Spark** 是可用时的 Fast Coding Worker，并优先使用其独立额度处理明确的小改、SQL、pandas/polars、简单 ETL、单测、lint、日志、文档、UI/adapter 小改和公式已经确定的因子实现。当前 Agent 调度工具没有列出 Spark 时，**不得声称已调度、不得让交付依赖它**；改用 Terra 或主模型并说明替代。
- **GPT-5.6 Terra** 是默认 Implementation Engineer：承担中等规模开发、较大 ETL、Data Adapter、Feature Pipeline、API/数据库、测试体系和已定架构的批量修改。工作量很大但逻辑明确时仍优先拆给 Terra/Spark，不因规模自动升级 Astra。
- **GPT-5.6 Sol Medium** 是目标 Tech Lead / Senior Engineer：负责 repository 理解、跨模块设计、schema、数据/因子/回测/组合/风险/执行架构、任务拆分、集成和 Review。标准复杂流程为 `Sol 分析与拆解 → Spark/Terra 实现并自测 → Sol 集中 Review`。
- **GPT-5.6 Sol High** 仅用于难定位 bug、多模块异常、性能/并发、复杂数据库、回测异常、PIT/泄漏、时间戳与数据对齐等高风险调试或审查，不是普通开发默认值。
- **GPT-6 Astra Low/Medium** 是 Quant Research Architect：用于新因子经济逻辑、基本面/另类数据、产业因果链、预期差、因子正交化/失效机制、regime、研究方法审查和重大架构不确定性。优先产出可验收的 `Research Specification`，再由 Sol 拆解、Spark/Terra 实现，而不是让 Astra 扫全库、写大量代码、跑普通测试或修常规 bug。
- **GPT-6 Astra High/Ultra 默认禁止**。只有 Sol High 无法可靠解决、问题对系统或研究结论影响重大、确有高阶推理需求且预期价值显著时才可建议；使用前必须向用户明确 `WHY_ASTRA_HIGH`、`EXPECTED_VALUE`、`WHY_SOL_IS_NOT_ENOUGH` 并取得确认。Ultra 只用于极少数系统级难题。
- 模型名称不是能力证明：对外部框架、行情源、回测器或交易连接器，必须以当前代码、版本、测试和实际能力登记为准。

### 角色与 reasoning

| 角色 | 默认模型/推理 | 职责与交付 |
|---|---|---|
| Tech Lead / 主控 | `gpt-5.6-sol` medium，或当前主模型的等价档 | 明确范围、接口、验收和风险；拆解任务、审查关键 diff/测试并作最终决策。 |
| Fast Coding Worker | Spark low/medium（仅在实际可用时） | 明确、局部、低研究风险的实现和测试；超出局部边界立即升级。 |
| Implementation Engineer | `gpt-5.6-terra` medium，复杂确定性实现可 high | 在明确文件所有权内实现、自测，汇报改动、命令结果与遗留项。 |
| Quant Researcher | `gpt-6-astra` low/medium；一般问题先用 Sol | 形成经济逻辑、数据口径、PIT、因子定义、失效条件和验证设计，不直接批量改代码。 |
| Factor / Backtest Engineer | Terra medium 或 Sol medium | 因子/回测实现、黄金样例、成本与账务；跨层设计由 Sol 负责。 |
| Risk Reviewer / Debugger | Sol high；达到高门槛后才建议 Astra high/ultra | 核对 PIT、样本切分、公司行为、成本、账务和声明边界；不以漂亮指标替代证据。 |

- 简单问答、单文件小改动不启动 Agent，也不做无关调研。较多实现工作默认先用**一个**实现 Agent；只有文件所有权不重叠、可独立推进的任务才增加第二个，不能为了并行而并行。
- 委派一次说明任务 ID、范围、不做什么、文件所有权、接口/数据约束、验收命令和报告格式；只传必要路径和摘要，不复制整段历史。主控不重复已具充分证据的实现者自测。
- 内部先判断 `TASK_TYPE / RESEARCH_RISK / ENGINEERING_RISK / RECOMMENDED_MODEL / REASONING_EFFORT`，不必每次问用户。只有 Astra High/Ultra、重大架构重构、不可逆数据模型修改、可能破坏既有研究结论或生产交易高风险修改需要先确认。
- 默认升级链为 `Spark → Terra → Sol Medium → Sol High → Astra Low → Astra Medium → Astra High → Astra Ultra`，不得无理由跳级。Spark/Terra 连续失败两次、开始触及多个核心模块或需要重设计时升级；Sol Medium 遇到 PIT/泄漏/回测正确性高风险时升级 Sol High；Sol High 仍有重大研究/架构不确定性才考虑 Astra。同一模型不得连续尝试同一种失败方案超过三次。
- 同一工具/路径连续失败两次，先判断原因，再用已授权替代方案或报告具体阻碍；不得盲目重试、重置、轮询或静默扩大范围。实现者遇到数据泄漏、账务不平、能力未验证、权限/密钥/真实账户、冲突改动时立即停在安全边界并升级主控。

## 3. 开工、上下文与变更范围

### 修复意图与任务连续性

- 在正在开发、维护本项目的语境中，用户报告 bug、截图异常、功能不能用或指出实现不符合需求，默认视为要求修复；先定位，再在已授权范围内实现、测试并验收，不得只解释原因、列建议或等用户再次说“修”。用户明确要求“仅分析/解释/审查、不要修改”时，只做诊断，不实施修复；必要授权或重大风险边界仍须确认。
- 每轮接回用户的完整目标和此前未完成事项。用户指出中途问题通常是原任务的补充，不是撤销原目标；修复后继续原任务。不得把用户举的例子、试验窗口或小样本数量擅自变成完整验收范围，范围有实质歧义时澄清。
- 小样本验证、分批限额、隔离测试和节省额度是实施手段，不是缩减交付目标的理由。试验成功后须推进已授权的完整流程；必要限额应说明原因并安排可恢复的续做，而非停在首批。禁止为了加快进度绕过供应商限流、数据/研究或安全门禁。
- 修复须检查直接相关链路：例如采集故障要核对网络/代理、实际下载、落盘、索引和页面消费；不能用某个服务 active 代替端到端成功。只检查与目标直接相关的环节，不扩展为无关整库审计或重构。
- 在安全、授权、工具及验收允许继续时，不因一个子步骤完成而结束工作。长任务使用真实后台任务和适当跟进机制，保留待办、实际覆盖、失败与恢复状态；明确区分“已启动”“部分完成”“全部验收完成”。确有阻碍时报告证据、已尝试的替代与所需决定，不假装完成，也不无限重试。
- 最终汇报优先给出已修复/未修复、验收证据、已部署/未部署以及剩余工作；解释原因不能替代实施交付。“省用量”“最小修改”限制无关开销，不限制完成用户已授权目标所必需的工作。

- 开工先用简短计划界定：做什么、不做什么、完成判据；界面任务同时明确布局、交互/选中、加载/空/失败状态、配色和可读性。
- 表单必须显式标记必填/必选项（统一使用可访问的红色 `*` 或等价语义），提交时执行与服务端契约一致的前端校验，并在字段附近说明具体缺项/范围且聚焦首个错误。除“正在提交”、防重复提交或不可逆安全门禁外，不得用无解释的禁用按钮代替校验；后端仍须独立 fail-closed 校验，前端提示不能成为放宽数据或研究约束的理由。
- 按任务读 `AGENTS.md`、对应 `TODO.md` 条目和依赖记录；用 `rg` 定位后阅读必要片段。除指令文件外，不重读整份历史文档，不全库扫描来替代具体问题定位。
- 实现前核对实际安装版本、已有接口和数据契约。说明、README、UI 文案和能力声明必须与代码一致；不凭印象批量生成后逐项返工。
- 输出只保留必要字段、计数、diff 与错误摘要；不输出密钥、完整凭证、无关浏览器标签或大段日志。浏览器优先语义定位和局部检查，截图只用于视觉验收/定位。
- 用户已有改动、原始行情、密钥和可审计工件必须保留。工作树当前缺少可靠 Git 基线的风险应被尊重：没有可比 HEAD 时，`git diff` 为空不等于无改动；不能覆盖或清理未知文件。
- 只做当前任务。发现历史问题记录并报告，不顺手扩成全库重构、文档全量更新或部署。范围明显膨胀时提出取舍。

## 4. 量化研究红线

### PIT、身份与数据

- **PIT/未来函数**：策略、特征、标签和回测只能读取决策时刻已可用的数据。事件至少保留并校验 UTC `event_time`、`available_time`、`ingest_time`、来源、修订和 schema 版本；不得用后来修订值回填早期决策。`src/quant_data/time_model.py` 与 `strategies.py` 的 fail-closed 可用时点语义不得绕过。
- ticker/symbol 不是稳定主键。证券、交易所、币种、合约规则、有效期及代码复用使用稳定 `instrument_id`/生命周期与版本；按历史生效时间取规则，不能把今天规则套进历史。
- 原始数据只追加、不可原地覆盖；供应商与 namespace 隔离，不能静默混源。清洗、映射、复权、特征和报告输出新版本，并保留输入 manifest、校验和、时间范围、来源、质量规则和代码/公式版本。
- 缺失必须区分休市、停牌、未上市、供应商缺口和未知；不可把缺失成交量/价格前向填充为可交易事实，也不可把供应商失败当零收益。

### 公司行为、退市与市场规则

- 股票必须清楚区分原始成交价、复权研究价和总回报；拆分、分红、改名、并购、退市现金/换股结算需可追溯。最终兑付缺失时警告或阻断，绝不把最后价格或零回报伪装为完整退市收益。
- 日线/OHLC 不能推断唯一盘中路径；同 bar 的止盈止损等冲突需公开保守规则或更细粒度数据。费用、点差、滑点、成交量约束、借券/融资、保证金与汇率应分别建模和报告，不能用一个滑点掩盖全部假设。
- 期货连续序列只能作研究信号；成交须落到真实到期合约，换月按平旧/开新、费用、价差和保证金记账。国内期货的夜盘、平今/开平、涨跌停和结算，及加密的 mark/index/last、资金费、线性/反向、保证金/清算均需逐产品验证，不能套用股票规则。
- 因子“可计算”、公式已校准、数据足够、样本外有效是不同状态。横截面与单资产使用匹配评估；训练/标准化/中性化参数只在训练段拟合；处理标签重叠泄漏；保留失败实验、搜索规模、样本外/滚动/敏感性/成本压力结果，不能只展示最优 Sharpe。

### 基本面与另类数据

- 对产量、出货量、销量、库存、订单、价格/ASP、开工率、渠道库存、出口、装机、CapEx、招聘、招标、供应链、卫星、舆情、新闻和管理层指引，不得未经口径/PIT/预期校验就把原始水平值直接当因子。
- 优先研究 YoY、QoQ、加速度、偏离度、Surprise、Consensus/Guidance/Inventory/Supply-Demand Gap、量价背离、历史分位和横截面标准分；同时记录行业季节性、修订、发布延迟、覆盖偏差与市场已知程度。
- 研究目标优先表达为 `Reality - Market Expectation`，而不是“公司很好”。若拿不到当时可用的一致预期或可靠代理，必须降低结论强度，不能用事后预期代替。

### 当前项目门禁

- 当前核心是美股日线研究数据与配置/任务骨架；`src/quant_data/backtest.py` 只允许合成 `synthetic-us-daily-v1` 的日线限价黄金路径。真实正式回测仍因合格 P3-03A 资产池、完整公司行为/退市适用性等缺失而**必须阻断**。
- `factor_jobs.py` 只有管理员配置的固定、合格快照才能产生研究收益；代表性 P1-04 公司行为样本不等于全市场总回报或合格研究池。
- NautilusTrader 1.221.0 仅有窄范围合成美股日线限价、部分成交、固定费用证据；市价单、真实行情、完整成本/公司行为/退市、其他市场与实盘能力均不得声称已验证。
- 回测/研究/模拟/外部 paper/实盘相互隔离。当前不开放真实下单；策略不可绕过风控或直接调用券商，浏览器不可获得任意代码执行或密钥。

## 5. 研究生命周期与目录现实

1. `Hypothesis → Economic Rationale`：提出可证伪假设，定义因果链、预期差、适用/失效机制、市场、频率、资产池、基准与成功/失败标准。
2. `Data Definition → PIT Validation`：发布固定 `DatasetSnapshot`，锁定历史成员、稳定身份、字段/单位、`period_end`、`announcement_date`、`publication_time`、`available_at`、`effective_at`、数据/质量/日历/公司行为版本、许可与 manifest；不使用 `latest/current/unknown` 等可变版本。
3. `Factor Construction → Unit Test`：锁定公式、参数、输入快照、可用时点和实现版本；验证边界、缺失、分母、方向、量纲、窗口和未来信息。
4. `Backtest → IC/Rank IC/IR → Turnover/Cost`：使用与横截面或时序问题匹配的评价，报告覆盖、分组/多空、衰减、换手、费用、点差、滑点、容量和不可交易约束；高收益不等于有效。
5. `Subperiod → Industry/Size Neutralization → Regime → Robustness`：中性化只在经济含义适用且参数仅从训练段拟合时使用；进行子区间、市场状态、参数敏感性与数据扰动检查。
6. `Out-of-Sample → Paper Trading → Production`：采用 train/validation/test 或 walk-forward，保留真正未参与调参的样本外集。若流程反复“调参→回测→看收益→再调参”，必须警告 backtest overfitting，并记录搜索空间和试验次数。
7. 策略版本锁定策略、因子、资产池、数据、日历、成本和风险配置；能力登记未满足则阻断提交。仅在合格数据、黄金样例、成本/账务核对和报告满足后进入受控回测；本地模拟、外部 paper、生产各自重新验收，绝不自动升级权限。

目标数据层为 `raw → normalized → curated/research snapshot → features → artifacts`。**当前并未物理拆分成 `research/` 与 `production/` 目录**：不要为“目录整洁”移动现有文件或假设隔离已存在。过渡期以只读原始路径、独立派生输出、固定 manifest/校验和、版本 ID、运行环境配置和权限/任务门禁实现逻辑隔离；迁移只能通过发布新目录/新快照完成，旧数据保留可追溯。

## 6. 代码、测试、Review 与发布

- 保持模块合同：Data 不伪造行情；Research/Factor 不宣称有效策略；Strategy 只产生信号/目标仓位；Risk 独立于策略并在发单前/持续检查；Execution 不扩大风险预算；OMS/Gateway 负责幂等和恢复；Ledger 以可审计事件而非图表累计值为准。每个账户只能有一个订单写入者。
- 代码遵循现有 Python 3.10+、`src/quant_data` 包和 pytest 配置；金额/数量/账务边界使用 `Decimal` 或等价定点语义。公共 API、schema、版本、路径、错误与状态变化必须有最小覆盖测试；避免无关重构。
- 实现者先做与风险匹配的专项测试：局部逻辑跑对应 pytest；影响公共契约、PIT、账务、数据写入或 API 时补充必要回归。相同版本/环境/条件已通过的测试不重复跑；新改动只补测受影响范围。可选依赖跳过、构建阻断或未做浏览器流程必须如实说明，不能算通过。
- Review 一次集中完成：核对范围/diff、数据与版本血缘、PIT/泄漏、身份/公司行为/成本/账务、错误 fail-closed 行为、测试证据和文档声明。意见合并成一批；修正后只复查受影响内容。重大问题可继续迭代，但不无限润色。
- 发布前在隔离目录验证，保留备份与回滚信息，再一次切换并做健康检查；不因每个样式改动重启线上服务。新依赖放独立环境，不升级/替换系统 Python 来迁就框架。写操作、外部连接和部署遵守权限，密钥不进仓库/API/日志。

### Definition of Done

一项任务只有在以下条件满足时才算完成：范围与验收项完成；数据/安全/研究门禁没有被绕过；代码与测试（及必要构建/代表性 UI 流程）有真实证据；任务记录含改动文件、验证命令/结果、部署/回滚信息（如适用）和遗留/阻断；`TODO.md` 仅在验收完成后更新状态。未验证能力、未部署差异和无权限步骤明确保留为未完成。

## 7. 汇报与每次自检

- 中途仅在阶段变化、重要发现或需用户决定时更新；最终说明结果、入口、必要测试和未解决问题，不逐条罗列工具调用。
- 不编造 token、额度、覆盖率、回测能力、数据完整性或“生产可用”结论；耗时不等于模型用量。明确区分代码存在、测试通过、部署完成和端到端验收。

完成前自检：

1. 是否只做了当前授权范围，并保护了原始数据、密钥和已有改动？
2. 是否用最小必要上下文/调用取得了可复核证据？
3. PIT、泄漏、身份、公司行为、退市、成本与能力门禁是否正确 fail-closed？
4. 是否由合适角色完成实现与独立审查，且没有重复已有充分证据？
5. 是否达到 Done 后及时停止，并如实列出阻断与遗留？
